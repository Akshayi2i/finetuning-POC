__pycache__/
*.pyc
.venv/
data/input.pdf
data/gold.json
data/sample_input.pdf
data/sample_gold.json

# Derived training inputs - regenerable from data/, and large (page renders).
training dataset/ocr/
training dataset/dataset/*.jsonl

# Model weights live outside the project entirely (paths.adapter_dir /
# paths.merged_root, /workspace/models on RunPod), so nothing to ignore here.

# Kept in git - small, and the evidence the POC produces:
#   results/**                                   extractions, meta, comparison
#   training dataset/dataset/dataset_meta.json   which documents were trained on
!**/.gitkeep
